using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.XR;
using UnityEngine.SceneManagement;
using UnityEngine.UI;
using UnityEngine.Rendering;
using UnityEngine.XR.Management;

public class HMDInfoManager : MonoBehaviour
{/*
            //Break if data is at end of show
            if (currentTimePos >= playedshowdata.Length)
            {
                break;
            }






                            //Slowed Down Speed
                if (inputSpeed == 0.5f)
                {
                    if (currentSongTime % 2 == 0)
                    {
                        currentSongTime += Time.deltaTime;
                        currentTimePos++;
                    }
                    else
                    {
                        currentTimePos = pauseResetTick;
                    }
                }
                //Negative speeds
                else if (inputSpeed < 0)
                {
                    currentSongTime -= Time.deltaTime;
                    currentTimePos = pauseResetTick;
                    currentTimePos -= 2;
                    if (currentTimePos < 0)
                    {
                        currentTimePos = 1;
                        FFSong(0);
                    }
                    while (playedshowdata[currentTimePos] != 0)
                    {
                        currentTimePos--;
                        if (currentTimePos < 0)
                        {
                            currentTimePos = 1;
                            FFSong(0);
                            break;
                        }
                    }
                    currentTimePos++;
                }
                //If any speed at 1 or above, continue to next frame.
                //Because this is inside a for loop, higher speeds
                //don't need its own else if statement
                else
                {
                    currentTimePos++;
                }
    */
}
